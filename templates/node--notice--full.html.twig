{% set classes = [
  'message',
  'is-' ~ notice_type|clean_class,
] %}

<article{{ attributes.addClass(classes) }}>
  <div class="message-body">
    {{ content.body }}
  </div>
  {{ title_suffix }}
</article>
